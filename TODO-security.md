# TODO: безопасность сессий (сделать позже)

Заметка на будущее. Сейчас всё работает, это про «когда дойдут руки».

---

## Что уже сделано

В `api/_verify.js` проверяется подпись Telegram (`initData`). Это главное:
без неё кто угодно мог отправить чужой `telegram_id` и накрутить себе
статистику или нагенерить счетов на оплату. Теперь id берётся только
из подписанных данных.

Там же стоит срок годности — сейчас **24 часа**:

```js
const MAX_AGE_SECONDS = 60 * 60 * 24;
```

---

## Что такое «срок годности» и зачем он

`initData` — строка, которую Telegram выдаёт игре при открытии.
Внутри неё лежит подпись, доказывающая, что данные настоящие.

Проблема: подпись сама по себе не устаревает. Если строку один раз
перехватили — её можно слать на сервер вечно, и он будет верить.

Поэтому внутри есть поле `auth_date` — момент выдачи. Сервер смотрит,
не слишком ли оно старое, и отказывает если да. Украденная строка
перестаёт работать через заданное время.

**Важно:** строка выдаётся один раз при открытии игры и сама не
обновляется. Пока вкладка открыта — она та же. Значит срок годности =
максимальная длина сессии.

---

## Задача 1: снизить срок до 1 часа

24 часа — это дефолт большинства библиотек, но для нас много.
Партия в ханафуду — минут двадцать. Час покрывает даже «открыл,
отвлёкся, вернулся».

**Правка в `api/_verify.js`:**

```js
const MAX_AGE_SECONDS = 60 * 60; // 1 час
```

---

## Задача 2: обработать протухание на клиенте (обязательно вместе с №1)

Если срок вышел, сервер вернёт **401**. Сейчас `apiFetch` этого не
заметит — запрос молча провалится в `catch`, и игра просто перестанет
сохраняться. Человек доиграет партию и потеряет результат, не поняв почему.

Надо: ловить 401 и показывать сообщение в игре, что-то вроде
«Сессия устарела — закройте и откройте игру заново».

Примерно так, в `apiFetch`:

```js
async function apiFetch(path, options = {}) {
  const headers = Object.assign(
    { 'Content-Type': 'application/json' },
    options.headers || {},
    TG_INIT_DATA ? { 'X-Telegram-Init-Data': TG_INIT_DATA } : {}
  );
  const r = await fetch(`${API_BASE}${path}`, Object.assign({}, options, { headers }));
  if (r.status === 401) {
    SESSION_EXPIRED = true;          // новый флаг
    G.msg = 'Сессия устарела — переоткройте игру';
    draw();
  }
  return r;
}
```

И дальше при `SESSION_EXPIRED` не долбить сервер повторными запросами
(в `persistState` и `saveMatchResult` сразу выходить).

---

## Задача 3 (не сейчас): JWT-сессии

Взрослая схема, стандартная для мини-аппов. Нужна будет, когда появятся
бустеры за реальные звёзды и списание баланса.

Как работает:
1. При старте игра один раз отправляет `initData` на `/api/auth`.
2. Сервер проверяет подпись и выдаёт свой токен (JWT) минут на 15–30.
3. Дальше все запросы ходят с этим токеном, а `initData` по сети
   больше не летает вообще.

Плюсы: у каждого токена короткое окно; сессию можно отозвать;
в токен можно положить свои поля (баланс звёзд, роль игрока).

Минус: это отдельный слой — надо хранить и обновлять токены.
Сейчас у нас три функции без состояния, для игры против ИИ это лишнее.

---

## На заметку: третья сторона

В `initData` есть поле `signature` — вторая подпись, Ed25519.
Её можно проверить публичным ключом Telegram, зная только `bot_id`,
без токена бота. Нужна, если данные надо будет отдать кому-то ещё.
Нам сейчас незачем.

---

## Мелочь про CORS

В `_verify.js` есть белый список доменов (`applyCors`). Раньше стояло
`'*'` — то есть любой сайт мог дёргать наш API из браузера игрока.

Защита слабая: из скрипта или curl CORS обходится, он работает только
в браузере. Но лишний слой не мешает. Если поменяется адрес Pages —
не забыть обновить список.
